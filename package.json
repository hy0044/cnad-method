{
  "name": "cnad-method",
  "version": "0.1.3",
  "description": "Context-Native AI Development repository integration CLI",
  "license": "MIT",
  "type": "module",
  "bin": {
    "cnad": "dist/cnad.js"
  },
  "files": [
    "dist",
    "templates",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@types/node": "^26.6.1",
    "eslint": "^9.39.5",
    "prettier": "^3.6.2",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.70.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hy0044/cnad-method.git"
  }
}
